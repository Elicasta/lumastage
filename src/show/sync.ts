import type{LumaApp}from"./sharedContract";export type SyncState="offline"|"loading"|"loaded"|"missing"|"conflict"|"error";export interface AppSyncStatus{app:LumaApp;state:SyncState;showId?:string;revision?:number;detail?:string;updatedAt:number}export interface SharedShowAck{type:"shared-show.ack";protocol:"shared-show-v1";showId:string;app:LumaApp;revision:number;state:"loaded"|"missing"|"conflict"|"error";detail?:string;timestamp:number}export function isSharedShowAck(x:unknown):x is SharedShowAck{if(!x||typeof x!=="object")return false;const v=x as Partial<SharedShowAck>;return v.type==="shared-show.ack"&&v.protocol==="shared-show-v1"&&typeof v.showId==="string"&&typeof v.app==="string"&&typeof v.revision==="number"}export class ShowSyncTracker{private states=new Map<LumaApp,AppSyncStatus>();begin(showId:string,revision:number){for(const app of["lumarig","lumaviz","lumastage","lumastudio"]as LumaApp[])this.states.set(app,{app,state:app==="lumastage"?"loaded":"loading",showId,revision,updatedAt:Date.now()})}receive(x:SharedShowAck){this.states.set(x.app,{app:x.app,state:x.state,showId:x.showId,revision:x.revision,detail:x.detail,updatedAt:x.timestamp})}all(){return[...this.states.values()]}}export const showSyncTracker=new ShowSyncTracker();