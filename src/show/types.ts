import type{EnergyVariant}from"../scenes/types";import type{TransitionSpec}from"../engine/gpu/types";export interface SectionCue{id:string;sectionId:string;sectionName:string;sceneId:string;variant:EnergyVariant;transition:TransitionSpec;locked:boolean;notes?:string}export interface SongShow{id:string;songId:string;songName:string;defaultSceneId:string;defaultVariant:EnergyVariant;sectionCues:SectionCue[];updatedAt:number;version:1}export interface ShowFile{id:string;name:string;songs:SongShow[];createdAt:number;updatedAt:number;version:1}