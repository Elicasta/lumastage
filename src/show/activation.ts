import type{SharedShowSnapshot}from"./sharedContract";export interface ShowActivation{type:"shared-show.activate";protocol:"shared-show-v1";showId:string;revision:number;locationId?:string;requestedBy:"lumastage";timestamp:number;mode:"load-only"}export function activationFor(snapshot:SharedShowSnapshot):ShowActivation{return{type:"shared-show.activate",protocol:"shared-show-v1",showId:snapshot.show.id,revision:snapshot.show.revision,locationId:snapshot.show.locationId,requestedBy:"lumastage",timestamp:Date.now(),mode:"load-only"}}