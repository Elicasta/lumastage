import{describe,it,expect}from"vitest";import{fromLegacyStudioMedia,isShowBusFrame}from"./showBus";describe("show bus",()=>{it("converts the real Studio media feed into semantic show state",()=>{const frame=fromLegacyStudioMedia({type:"lumastudio.media",version:1,timestamp:1000,positionSeconds:12.5,playing:true,sectionId:"section-2",sectionName:"Chorus",sectionIndex:1,song:{id:"song-1",name:"Example Song",artist:"Artist",durationSeconds:240,bpm:120}});expect(frame).toMatchObject({type:"luma.show",version:1,source:"lumastudio",timestamp:1000,song:{id:"song-1",name:"Example Song",artist:"Artist",positionSeconds:12.5,durationSeconds:240,bpm:120,playback:"playing"},section:{id:"section-2",name:"Chorus",index:1}});});it("rejects malformed timestamps and song payloads",()=>{expect(isShowBusFrame({type:"luma.show",version:1,source:"lumastudio",timestamp:NaN})).toBe(false);expect(isShowBusFrame({type:"luma.show",version:1,source:"lumastudio",timestamp:1,song:{name:"Song",positionSeconds:-1,playback:"playing"}})).toBe(false);expect(isShowBusFrame({type:"luma.show",version:1,source:"unknown",timestamp:1})).toBe(false);});it("adapts existing LumaStudio media frames",()=>{const x=fromLegacyStudioMedia({timestamp:10,positionSeconds:12,playing:true,sectionId:"chorus"});expect(isShowBusFrame(x)).toBe(true);expect(x.section?.id).toBe("chorus");expect(x.song?.playback).toBe("playing")});it("rejects wrong protocol versions",()=>expect(isShowBusFrame({type:"luma.show",version:2,timestamp:1})).toBe(false));});