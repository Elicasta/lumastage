import type{StageProgramFrame,StageVizFrame}from"./showBus";import{BrowserStageTransport}from"./transport";import{SocketStageTransport}from"./socketTransport";import type{SharedShowSnapshot}from"../show/sharedContract";import type{ShowActivation}from"../show/activation";import{showSyncTracker}from"../show/sync";import{NativeStudioTransport}from"./nativeStudioTransport";export interface RuntimeTransport{start():()=>void;publishProgram(x:StageProgramFrame):void;publishViz(x:StageVizFrame):void;publishSharedShow(x:SharedShowSnapshot):void;activateShow(x:ShowActivation):void}export class HybridStageTransport implements RuntimeTransport{private browser=new BrowserStageTransport();private socket=new SocketStageTransport();private nativeStudio=new NativeStudioTransport();start(){const a=this.browser.start(),b=this.socket.start(),c=this.nativeStudio.start();return()=>{a();b();c()}}publishProgram(x:StageProgramFrame){this.browser.publishProgram(x);this.socket.publishProgram(x)}publishViz(x:StageVizFrame){this.browser.publishViz(x);this.socket.publishViz(x)}publishSharedShow(x:SharedShowSnapshot){this.socket.publishSharedShow(x);window.dispatchEvent(new CustomEvent("luma:shared-show:snapshot",{detail:x}))}activateShow(x:ShowActivation){showSyncTracker.begin(x.showId,x.revision);this.socket.send(x);window.dispatchEvent(new CustomEvent("luma:shared-show:activate",{detail:x}))}}export const runtimeTransport=new HybridStageTransport();