import type{RenderLayer}from"../engine/renderGraph";import type{Modulation}from"../engine/modulation";export type EnergyVariant="low"|"mid"|"high"|"peak";export interface SceneVariant{id:EnergyVariant;label:string;overrides:Record<string,number|string|boolean>}export interface StageScene{id:string;name:string;collection?:string;tags:string[];width:number;height:number;background:string;layers:RenderLayer[];modulations:Modulation[];variants:SceneVariant[];defaultVariant:EnergyVariant;createdAt:number;updatedAt:number;version:1}export interface SceneSelection{sceneId:string;variant:EnergyVariant}