import{describe,it,expect}from"vitest";import{SceneBuilder}from"./sceneBuilder";import{sceneLibrary}from"./library";describe("scene builder",()=>it("edits a copy without mutating the library",()=>{const original=sceneLibrary[0].name;const b=new SceneBuilder(structuredClone(sceneLibrary[0])).rename("New");expect(b.snapshot().name).toBe("New");expect(sceneLibrary[0].name).toBe(original)}));