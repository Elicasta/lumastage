import{describe,it,expect}from"vitest";import{TransitionEngine}from"./transitionEngine";describe("transition engine",()=>{it("is deterministic from timestamps",()=>{const x=new TransitionEngine();x.begin("a","b",{kind:"dissolve",durationMs:1000,easing:"linear"},100);expect(x.progress(600)).toBe(.5);expect(x.active(1099)).toBe(true);expect(x.active(1100)).toBe(false)});it("supports smooth easing",()=>{const x=new TransitionEngine();x.begin("a","b",{kind:"dissolve",durationMs:1000,easing:"ease-in-out"},0);expect(x.progress(250)).toBeCloseTo(.15625)})});