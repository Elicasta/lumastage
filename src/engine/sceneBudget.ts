import type{CompiledScene}from"./sceneCompiler";export interface SceneBudget{maxPasses:number;maxPixels:number}export interface BudgetResult{ok:boolean;passes:number;pixels:number;messages:string[]}export function checkSceneBudget(scene:CompiledScene,budget:SceneBudget={maxPasses:18,maxPixels:3840*2160}){const pixels=scene.width*scene.height,messages:string[]=[];if(scene.estimatedPasses>budget.maxPasses)messages.push("Scene requires "+scene.estimatedPasses+" render passes; budget is "+budget.maxPasses+".");if(pixels>budget.maxPixels)messages.push("Scene resolution exceeds configured pixel budget.");return{ok:messages.length===0,passes:scene.estimatedPasses,pixels,messages}:BudgetResult}