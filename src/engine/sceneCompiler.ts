import type{RenderScene,RenderLayer}from"./renderGraph";import type{EffectParams,GeneratorParams}from"./gpu/types";export interface CompiledLayer{source:RenderLayer;generator?:GeneratorParams;effects:EffectParams[]}export interface CompiledScene{id:string;width:number;height:number;layers:CompiledLayer[];estimatedPasses:number}export function compileScene(scene:RenderScene):CompiledScene{const layers=scene.layers.filter(x=>x.enabled&&x.opacity>0).map(source=>({source,generator:source.generator?{kind:source.generator.kind as GeneratorParams["kind"],seed:source.generator.seed,speed:source.generator.speed,energy:source.generator.energy}:undefined,effects:source.effects.filter(x=>x.amount>0).map(x=>({id:x.id,kind:x.kind as EffectParams["kind"],enabled:true,amount:x.amount}))}));return{id:scene.id,width:scene.width,height:scene.height,layers,estimatedPasses:layers.reduce((n,x)=>n+1+x.effects.length,0)}}