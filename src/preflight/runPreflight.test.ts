import{describe,it,expect}from"vitest";import{runPreflight}from"./runPreflight";const base={studioConnected:true,vizConnected:true,rendererReady:true,fps:60,frameTimeMs:16.6,droppedFrames:0,transportLatencyMs:8,media:[],outputs:[{id:"main",name:"Main",enabled:true,available:true,width:1920,height:1080,fps:60}]};describe("preflight",()=>{it("is ready for a healthy show",()=>expect(runPreflight(base).status).toBe("ready"));it("blocks missing required media",()=>expect(runPreflight({...base,media:[{id:"x",name:"Intro",ready:false,codecSupported:true,required:true}]}).status).toBe("blocked"));it("allows manual show with Studio offline",()=>expect(runPreflight({...base,studioConnected:false}).status).toBe("ready-with-warnings"));it("blocks an unavailable enabled output",()=>expect(runPreflight({...base,outputs:[{...base.outputs[0],available:false}]}).status).toBe("blocked"));it("blocks when no external output is armed",()=>expect(runPreflight({...base,outputs:[{...base.outputs[0],enabled:false}]}).status).toBe("blocked"));});