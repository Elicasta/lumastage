export type PreflightSeverity="blocker"|"warning"|"info";export type PreflightArea="transport"|"media"|"renderer"|"outputs"|"performance"|"recovery";export interface PreflightFinding{id:string;area:PreflightArea;severity:PreflightSeverity;title:string;detail:string;action?:string}export interface PreflightSnapshot{studioConnected:boolean;vizConnected:boolean;rendererReady:boolean;fps:number;frameTimeMs:number;droppedFrames:number;transportLatencyMs:number;media:Array<{id:string;name:string;ready:boolean;codecSupported:boolean;required:boolean}>;outputs:Array<{id:string;name:string;enabled:boolean;available:boolean;width:number;height:number;fps:number;expectedWidth?:number;expectedHeight?:number}>;sharedShow?:{id:string;revision:number;locationId?:string;rigDocument:boolean;vizDocument:boolean;stageDocument:boolean}}export interface PreflightReport{status:"ready"|"ready-with-warnings"|"blocked";checkedAt:number;findings:PreflightFinding[]}