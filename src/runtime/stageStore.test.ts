import{describe,it,expect,beforeEach}from"vitest";import{stageRuntime}from"./stageStore";import type{ShowBusFrame}from"../integrations/showBus";const frame=(section:string):ShowBusFrame=>({type:"luma.show",version:1,source:"lumastudio",timestamp:1,song:{name:"Test",positionSeconds:2,playback:"playing",bpm:72},section:{id:section,name:section},beat:{bar:1,beat:2}});describe("stage runtime",()=>{beforeEach(()=>stageRuntime.resumeAuto());it("auto follows Studio sections",()=>{stageRuntime.receiveShow(frame("Chorus"));expect(stageRuntime.get().programScene).toBe(2)});it("manual firing suspends auto follow",()=>{stageRuntime.goScene(7);stageRuntime.receiveShow(frame("Verse 1"));expect(stageRuntime.get().programScene).toBe(7);expect(stageRuntime.get().manualOverride).toBe(true)});it("resume auto restores section mapping",()=>{stageRuntime.goScene(7);stageRuntime.resumeAuto();stageRuntime.receiveShow(frame("Verse 1"));expect(stageRuntime.get().programScene).toBe(4)});it("blackout never changes playback state",()=>{stageRuntime.receiveShow(frame("Chorus"));stageRuntime.toggleBlackout();expect(stageRuntime.programFrame().playing).toBe(true)});});